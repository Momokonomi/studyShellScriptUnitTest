version: "3"
services:
    app:
        build:
            context: .
            dockerfile: ./Dockerfile
        container_name: shellTest
        working_dir: /home/devuser/src
        user: devuser
        volumes:
            - ../:/home/devuser/src
        tty: true
